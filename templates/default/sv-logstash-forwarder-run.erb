#!/bin/bash

exec 2>&1
exec chpst -u <%= @options[:user] %>:<%= @options[:group] %> \
  /opt/logstash-forwarder/bin/logstash-forwarder \
  -config /etc/logstash-forwarder \
  -spool-size <%= @options[:spool_size] %> <%= '-log-to-syslog' if @options[:log_to_syslog] %>
